import { Text, View } from "react-native";
import Header from "../components/Header";

export default () =>{
    return (
        <View>
            <Header />
            <Text>Tela principal</Text>
        </View>

    );
}