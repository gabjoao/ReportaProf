import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container:{
        alignItems: 'center',
        marginTop: 80,
        gap: 20,
    },

    h1: {
        fontSize: 24,
        fontFamily: 'Lexend',
        textAlign: 'center',
        height: 30,
    },
    h2: {
        fontSize: 20,
        fontFamily: 'Lexend',
        textAlign: 'center',
        
    },
});